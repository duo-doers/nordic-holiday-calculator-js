{
  "name": "nordic-holiday-calculator-js",
  "version": "0.4.0",
  "description": "A library for listing Nordic national holidays.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "pnpm typecheck && pnpm test && pnpm build",
    "release:patch": "pnpm version patch && git push && git push --tags",
    "release:minor": "pnpm version minor && git push && git push --tags",
    "release:major": "pnpm version major && git push && git push --tags"
  },
  "files": [
    "dist"
  ],
  "keywords": [],
  "author": "Jan Sobotka, Riku Eteläniemi",
  "repository": {
    "type": "git",
    "url": "git@github.com:Rikusor/nordic-holiday-calculator-js.git"
  },
  "license": "MIT",
  "packageManager": "pnpm@10.30.2",
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
